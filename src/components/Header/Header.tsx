import { NavBar } from '../NavBar/NavBar';
import { SortingBar } from '../SortingBar/SortingBar';

export const Header = () => {
  return (
    <>
      <NavBar />
      <SortingBar />
    </>
  );
};
